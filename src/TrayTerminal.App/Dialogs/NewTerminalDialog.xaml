<Window x:Class="TrayTerminal.App.Dialogs.NewTerminalDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新建标签"
        Width="420"
        Height="280"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBackgroundBrush}"
        Foreground="{StaticResource TextBrush}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="标题" Margin="0,0,0,6" />
        <TextBox x:Name="TitleTextBox"
                 Grid.Row="1"
                 Height="30"
                 Margin="0,0,0,14" />

        <TextBlock Grid.Row="2" Text="终端类型" Margin="0,0,0,6" />
        <ComboBox x:Name="ProfileComboBox"
                  Grid.Row="3"
                  Height="40"
                  VerticalAlignment="Top"
                  DisplayMemberPath="DisplayName" />

        <StackPanel Grid.Row="4" Margin="0,22,0,0">
            <CheckBox x:Name="AdminCheckBox"
                      Content="以管理员模式启动"
                      Margin="0,0,0,16" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton"
                        Content="取消"
                        Width="82"
                        Height="30"
                        Margin="0,0,8,0"
                        IsCancel="True" />
                <Button x:Name="OkButton"
                        Content="创建"
                        Width="82"
                        Height="30"
                        IsDefault="True" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
